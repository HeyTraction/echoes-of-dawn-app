{
  "name": "echoes-of-dawn-app",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "jest",
    "test:perf": "lighthouse http://localhost:3000 --quiet --chrome-flags='--headless' --output=json --output-path=./lighthouse.json"
  },
  "dependencies": {
    "next": "13.4.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "lighthouse": "^11.0.0"
  }
}
